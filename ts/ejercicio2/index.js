// Crear una función que convierta un string a mayúsculas.
function convierteMinMay(texto) {
    texto = texto.toUpperCase();
    return texto;
}
var texto = "prueba";
console.log(convierteMinMay(texto));
